#: The ALE a starting strain is filed under. Text since `mutint_experiment.0008` made
#: `Population.name` a `CharField` -- it is compared against values read back off the column,
#: so an `int` here would silently stop matching anything.
STARTING_STRAIN_ALE_ID = "0"

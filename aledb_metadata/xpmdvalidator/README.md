# xpmd-json
Python library to convert `XPMD` metadata CSV and validate them using `json-schema` (http://json-schema.org/). This module will be used by other applications for validation of metadata before they use it.

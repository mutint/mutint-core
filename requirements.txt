Django>=4.2,<5.0
asgiref==3.7.2
beautifulsoup4==4.12.2
coverage==4.5.3
cryptography==41.0.4
django-bootstrap3==23.4
django-bootstrap4==23.2
django-debug-toolbar==4.2.0
django-filter==23.2
django-tables2==2.6.0
djangoajax==3.3
# GenomeDiff (.gd) parser/writer used by the drag-and-drop mutation import.
# Pinned to a git SHA, not a PyPI version: only 0.4.1 was ever published there, and
# what this depends on landed after it -- the INT mutation type, the ordered
# Record.__str__ serializer gd_import.py and Mutation.to_gd_line() rely on, and now
# breseq's SC/MP/DP/PD/MASK entry types plus tolerance of ones it has not seen.
# Without that last part a current breseq output.gd fails to load in full.
#
# 43a0f72 adds genomediff.schema -- breseq's own field guards, mirrored from
# genome_diff_entry.cpp -- behind Record.get()/set()/validate(). aledb_mutation_editor
# checks hand-entered mutations against it rather than keeping a second opinion about
# what breseq accepts. It also stops a malformed line costing the whole file: problems
# collect in gd.parse_errors, which the import summary now reports per file.
#
# The SHA is the whole pin: every commit here reports itself as 0.4.2, so pip will not
# upgrade an already-installed 0.4.2 on the strength of the requirement alone. An
# existing environment needs
#   pip install --force-reinstall --no-deps 'git+https://github.com/barricklab/genomediff-python.git@43a0f72'
# (dev: `pip install -e /path/to/genomediff`)
genomediff @ git+https://github.com/barricklab/genomediff-python.git@43a0f72
# GenBank/FASTA parsing for uploaded reference genomes (aledb_import/reference.py).
# Note Biopython has no GFF3 reader -- that lives in the separate bcbio-gff package -- so
# breseq's data/reference.gff3 is read by a small in-repo parser instead.
biopython>=1.81
jsonfield==3.1.0
jsonschema==4.19.1
lxml==4.9.3
numpy==1.25.2
oauthlib==3.2.2
packaging==23.2
pandas==2.1.0
python-dateutil==2.8.2
requests==2.31.0
six==1.11.0
sqlparse==0.4.4
urllib3==2.0.6
xlrd==1.1.0
zipstream-ng==1.8.0
